package piscine

// pieces are...
// P, B, R, Q

func ConvertBoard(board []string){
	// for i, s := range board {
	// 	for j, c := range s {
	// 		if c == 'P' {
	// 			pawn(board, i, j)
	// 		} else if c == 'B' {
	// 			bishop(board, i, j)
	// 		} else if c == 'R' {
	// 			rook(board, i, j)
	// 		} else if c == 'Q' {
	// 			queen(board, i, j)
	// 		}
	// 	}
	// }
}